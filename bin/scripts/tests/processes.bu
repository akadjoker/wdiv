var counter = 0;

process test() {
    counter = counter + 1;
    
    if (counter == 1) {
        pass("process executed");
    }
    
    if (counter == 5) {
        assert_eq(id, 0, "process id");
        pass("process completed");
        exit;
    }
    
    frame;
}

test();
 